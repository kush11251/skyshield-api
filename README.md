# SkyShield API
A high-performance threat detection and response system.

## Installation
1. Initialize the go module: `go mod init skyshield-api`
2. Install dependencies: `go get github.com/gorilla/mux`
3. Run the application: `go run src/main.go`